name: Build APK

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Build debug APK
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug --stacktrace

      - name: Rename APK
        run: cp app/build/outputs/apk/debug/app-debug.apk TouchMouse.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: TouchMouse-apk
          path: TouchMouse.apk

      - name: One-click release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v1.0.${{ github.run_number }}
          name: TouchMouse v1.0.${{ github.run_number }}
          body: |
            📲 **Installer en 1 clic : télécharge TouchMouse.apk ci-dessous et ouvre-le sur ton Android.**

            Souris virtuelle pour écran tactile cassé (bande gauche 25%) : curseur + pad + clic / long / sélection / drag / scroll / back / home.
          files: TouchMouse.apk
